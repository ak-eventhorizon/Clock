# Simple clock app

### Механика работы:

Функция createClock(elementID, elementHeight) принимает id элемента, в который нужно поместить часы и желаемую высоты элемента - остальные размеры вычисляются исходя из нее.